# Tools

Small helper scripts for development/testing.

- `make_seed.py`: copies image files into the blob store and appends a row to `seed.json`.

These scripts are not part of the protocol; they exist only to bootstrap local dev quickly.
