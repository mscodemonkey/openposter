# What I don't like so far

## Node claim
I don't like the node claim process.  I think the node itself should have its own UI that allows you to register yourself as the owner.  

### How it should work
- Create a username and password then create a passkey
- No node claim number required

- Then, from the main UI you just use your created credentials to login to the node.